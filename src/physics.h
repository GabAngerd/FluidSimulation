#include <stdio.h>
#include <math.h>
#include "circle.h"

void updatePosition(Circle* circles, int numCircles, float timestep);//(float* xPos, float* yPos, float* xVelocity, float* yVelocity,float time);